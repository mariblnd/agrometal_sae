

{% extends 'admin_base.html.twig' %}

{% block main %}
    <h1>Edit Equipments</h1>

    {{ include('equipments/_form.html.twig', {
        'button_label': 'Update',
        'form_action': path('app_equipments_edit', {'id': equipment.id})
    }) }}

    <a href="{{ path('app_equipments_index') }}">back to list</a>

    {{ include('equipments/_delete_form.html.twig') }}
{% endblock %}
