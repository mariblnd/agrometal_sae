<form method="post" action="{{ path('app_news_number_delete', {'id': news_number.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_number.id) }}">
    <button class="btn">Delete</button>
</form>
