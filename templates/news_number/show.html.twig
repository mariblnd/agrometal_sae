{% extends 'base.html.twig' %}

{% block title %}NewsNumber{% endblock %}

{% block body %}
    <h1>NewsNumber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ news_number.id }}</td>
            </tr>
            <tr>
                <th>Title_number</th>
                <td>{{ news_number.titleNumber }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ news_number.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ news_number.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_news_number_index') }}">back to list</a>

    <a href="{{ path('app_news_number_edit', {'id': news_number.id}) }}">edit</a>

    {{ include('news_number/_delete_form.html.twig') }}
{% endblock %}
