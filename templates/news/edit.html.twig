{% extends 'admin_base.html.twig' %}

{% block main %}
    <h1>Edit News</h1>

    {{ include('news/_form.html.twig', {
        'button_label': 'Update',
        'form_action': path('app_news_edit', {'id': news.id})
    }) }}

    <a href="{{ path('app_news_index') }}">back to list</a>

    {{ include('news/_delete_form.html.twig') }}
{% endblock %}
