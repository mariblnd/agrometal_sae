{% extends 'base.html.twig' %}

{% block title %}MapRegions{% endblock %}

{% block body %}
    <h1>MapRegions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ map_region.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ map_region.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ map_region.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_map_regions_index') }}">back to list</a>

    <a href="{{ path('app_map_regions_edit', {'id': map_region.id}) }}">edit</a>

    {{ include('map_regions/_delete_form.html.twig') }}
{% endblock %}
